package info.brightly.bright;

/**
 * Set of enums is used to define which mode the slider drawer is in, differentiating between
 * closed and all the other open states.
 *
 * @author Justin Ng (jn3141)
 */
enum SliderMode {
    // Slider panel is closed.
    CLOSED,

    // The three different tabs down the bottom.
    MAP, ADD, PURCHASE
}
